{
  "private": true,
  "scripts": {
    "test": "npm run test -workspace=services/items-api",
    "pack": "npm run pack -workspace=services/items-api",
    "init": "npm run init -workspace=terraform",
    "deploy": "npm run deploy -workspace=terraform",
    "destroy": "npm run destroy -workspace=terraform",
    "update": "npm run pack && npm run deploy"
  },
  "workspaces": [
    "services/items-api",
    "terraform"
  ]
}